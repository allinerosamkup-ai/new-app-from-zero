# Values used during web build stage.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
# Legacy name still accepted by the web client.
VITE_SUPABASE_ANON_KEY=

# Filled automatically in compose by default:
# VITE_API_URL=/api
