# Dependências
node_modules/

# Variáveis de ambiente (NUNCA commitar)
.env
.env.local
.env.production
.env.*
artifacts/.env*

# Build
dist/
build/
# Service worker de desenvolvimento — regenerado a cada `vite dev`
dev-dist/

# Anexos temporários de sessões de agente
.codex-remote-attachments/

# Expo
.expo/
apps/mobile/android/
vite.config.previewbridge.js

# D.U.M.M.Y. OS memory
.dummy/

# Estado por sessão do verification-guard (.claude/hooks/verification-guard.mjs)
.claude/.state/

# Configuração local do Claude Code — guarda tokens em `env` e permissões
# desta máquina. NUNCA versionar: já vazou EXPO_TOKEN e senha do Postgres uma vez.
.claude/settings.local.json

# Worktrees (isolated workspaces)
.worktrees/
worktrees/
.tmp/
tmp/
scratch/
**/*.cjs
**/*.js
# Mas permitir arquivos JS normais do projeto
!apps/web/src/**/*.js
!apps/backend/src/**/*.js
!apps/mobile/*.js
!apps/mobile/plugins/**/*.js
!packages/**/*.js
